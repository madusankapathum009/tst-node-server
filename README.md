# Simple Node Express Server

This is a basic Node.js application built with Express.

## How to run

1.  **Install dependencies** (if you haven't already):
    ```bash
    npm install
    ```

2.  **Start the server**:
    ```bash
    npm start
    ```

The server will be running at [http://localhost:3000](http://localhost:3000).
